#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int i,n;
    scanf("%d",&n);
    int a[n],b[n];
    for (i=0;i<n;i++){
     scanf("%d",&a[i]);
        //printf("%d ",a[i]);
    }
    for (i=0;i<n;i++){
        scanf("%d",&b[i]);
    }
    return 0;
}
